apiVersion: v1
kind: Service
metadata:
  name: hellp-app
  labels:
    run: hellp-app
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    run: hellp-app